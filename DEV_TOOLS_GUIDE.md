# 开发者工具使用指南

## 快速重置到登录页

当你在预览窗口中卡在欢迎页或其他任何页面时，可以使用开发者工具快速重置到登录页面。

### 使用步骤

1. **打开开发者工具面板**
   - 在预览窗口的右下角（底部导航栏上方），你会看到一个紫色的齿轮图标
   - 点击这个图标打开开发者工具面板

2. **点击"重置到登录页"按钮**
   - 在开发者工具面板顶部，有一个绿色的"重置到登录页"按钮
   - 点击这个按钮
   - 确认弹出的提示框

3. **等待页面刷新**
   - 系统会自动清除所有认证状态和引导流程数据
   - 页面将自动刷新并返回到登录页面

### 功能说明

**"重置到登录页"按钮会执行以下操作：**
- ✅ 清除所有引导流程状态（onboarding_step, onboarding_data等）
- ✅ 清除认证会话（Supabase signOut）
- ✅ 清除sessionStorage
- ✅ 自动刷新页面返回登录界面

**不会执行的操作：**
- ❌ 不会删除数据库中的用户数据
- ❌ 不会删除其他localStorage数据（如仪表盘设置等）

### 其他可用工具

开发者工具面板还提供了其他实用功能：

1. **重置测试数据（黄色按钮）**
   - 清除当前用户的所有健康记录和测试数据
   - 需要已登录状态
   - 不会退出登录

2. **退出登录（蓝色按钮）**
   - 仅退出登录，不清除任何数据
   - 只在已登录时显示

3. **清除所有数据（红色按钮 - 危险）**
   - 清除所有数据库数据
   - 清除所有localStorage
   - 退出登录并刷新页面
   - ⚠️ 这是最彻底的清理选项

### 状态信息

开发者工具面板顶部显示当前状态：
- **登录状态**：显示是否已登录
- **用户**：显示当前登录用户的标识
- **测试模式**：显示测试模式是否启用

### 注意事项

- 开发者工具仅在开发环境（`npm run dev`）中可见
- 在生产环境中不会显示
- 所有操作都会有确认提示，避免误操作

---

现在你可以随时使用"重置到登录页"按钮来快速测试登录功能了！
